#include <stdio.h>
#include <stdlib.h>

int main (int ac, char **av)
{
	int i = 0;
	printf("%d\n", ac);
	int b = 0;
	printf("%d\n", ac);
	if(ac > 1 && b == 0)
	{
		printf("%d\n", ac);
		for (i = 0; i < ac; i++)
	{

		if (av[ac] == NULL )
        {
                printf("%d\n", ac);
	}

	}
	}
	return (0);
}
